# 🎭 Карта событий города

Веб-приложение для отслеживания городских событий с автоматическим импортом с Яндекс.Афиши. Построено на FastAPI, PostgreSQL с PostGIS и Leaflet.

## ✨ Возможности

- **🎭 Автоматический импорт событий** с Яндекс.Афиши (концерты, театр, выставки, спорт)
- **📍 Отслеживание городских событий**: ремонты дорог, ДТП, мероприятия
- **🗺️ Интерактивная карта** с фильтрами по типу, дате и источнику
- **🤖 Telegram бот** с уведомлениями о событиях в подписанных районах
- **🏘️ Работа с районами**: границы, население, статистика событий
- **🔍 Геопространственные запросы**: поиск событий в радиусе, по районам

## 🎯 Типы событий

### Культурные (автоимпорт с Яндекс.Афиши):
- 🎵 Концерты
- 🎭 Театр
- 🖼️ Выставки
- ⚽ Спорт
- 🎪 Фестивали

### Городские (ручной ввод):
- 🚧 Ремонты дорог
- 🚗 ДТП
- 🏛️ Городские мероприятия

## 📁 Структура проекта

```
geo_2025/
├── backend/                    # FastAPI приложение
│   ├── app/
│   │   ├── main.py            # Точка входа
│   │   ├── database.py        # Подключение к БД
│   │   ├── models.py          # SQLAlchemy модели
│   │   ├── schemas.py         # Pydantic схемы
│   │   ├── routers/           # API эндпоинты
│   │   │   ├── events.py      # События
│   │   │   └── districts.py   # Районы
│   │   ├── bot/               # Telegram бот
│   │   │   ├── bot.py         # Основной модуль
│   │   │   ├── handlers.py    # Обработчики команд
│   │   │   ├── notifications.py # Уведомления
│   │   │   └── scheduler.py   # Планировщик
│   │   └── scrapers/          # Парсеры
│   │       └── yandex_afisha.py # Парсер Яндекс.Афиши
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/                   # Веб-интерфейс
│   ├── index.html
│   ├── css/style.css
│   ├── js/
│   │   ├── map.js             # Логика карты
│   │   └── api.js             # API клиент
│   ├── Dockerfile
│   └── nginx.conf
├── sql/                        # SQL скрипты
│   ├── init.sql               # Инициализация БД
│   ├── migration_remove_objects.sql # Миграция
│   ├── test_data.sql          # Тестовые данные
│   └── clear_data.sql         # Очистка
├── docker-compose.yml
├── .env
├── REFACTORING_PLAN.md        # План рефакторинга
└── REFACTORING_PROGRESS.md    # Прогресс рефакторинга
```

## 🚀 Быстрый старт

### Требования

- Docker
- Docker Compose

### 1. Клонирование репозитория

```bash
git clone https://github.com/Burunduke/geo_2025
cd geo_2025
```

### 2. Настройка переменных окружения

Файл `.env` уже настроен с базовыми параметрами:

```env
POSTGRES_USER=geo_user
POSTGRES_PASSWORD=geo_password
POSTGRES_DB=city_geo_db

BACKEND_HOST=backend
BACKEND_PORT=8000

# Telegram Bot (получите токен у @BotFather)
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
NOTIFICATION_HOUR=9
NOTIFICATION_MINUTE=0

# Yandex Afisha Integration
YANDEX_AFISHA_IMPORT_ENABLED=true
YANDEX_AFISHA_IMPORT_HOUR=2
YANDEX_AFISHA_IMPORT_MINUTE=0
YANDEX_AFISHA_CITY=voronezh
```

**Важно**: Для работы Telegram бота необходимо:
1. Создать бота через [@BotFather](https://t.me/botfather)
2. Получить токен и добавить его в `.env`
3. Подробная инструкция в [TELEGRAM_BOT_SETUP.md](TELEGRAM_BOT_SETUP.md)

### 3. Запуск приложения

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps
```

Приложение будет доступно по адресу: **http://localhost**

API документация: **http://localhost:8000/docs**

### 4. Загрузка тестовых данных

```bash
# Загрузить тестовые события и районы
docker-compose exec -T db psql -U geo_user -d city_geo_db < sql/test_data.sql
```

### 5. Остановка приложения

```bash
# Остановка сервисов
docker-compose down

# Остановка с удалением томов (полная очистка)
docker-compose down -v
```

## 📊 API Endpoints

### События (`/api/events`)

- `GET /api/events/` - Получить все события
  - Параметры: `event_type`, `source`, `upcoming_only`
- `GET /api/events/{id}` - Получить событие по ID
- `GET /api/events/filter/today` - События сегодня
- `GET /api/events/filter/upcoming` - Предстоящие события
  - Параметры: `days`, `limit`
- `GET /api/events/by-district/{id}` - События в районе
- `GET /api/events/nearby` - События в радиусе
  - Параметры: `lat`, `lon`, `radius`, `event_type`
- `POST /api/events/` - Создать событие
- `POST /api/events/import-afisha` - Импорт с Яндекс.Афиши
  - Параметры: `city`, `categories`, `days_ahead`, `limit_per_category`
- `GET /api/events/types` - Статистика по типам

### Районы (`/api/districts`)

- `GET /api/districts/` - Получить все районы
- `GET /api/districts/find` - Найти район по координатам
- `GET /api/districts/{id}/stats` - Статистика района
- `GET /api/districts/{id}/buffer` - Буферная зона
- `GET /api/districts/intersect` - Пересекающиеся районы

## 🗄️ База данных

### Структура таблиц

**events** - События
- `id` - Уникальный идентификатор
- `title` - Название
- `event_type` - Тип (concert, theater, exhibition, sport, festival, repair, accident, city_event)
- `description` - Описание
- `geom` - Геометрия (Point, SRID 4326)
- `start_time` - Время начала
- `end_time` - Время окончания
- `source` - Источник (yandex_afisha, manual, telegram)
- `source_url` - Ссылка на источник
- `image_url` - URL изображения
- `price` - Цена
- `venue` - Место проведения
- `created_at` - Дата создания

**districts** - Районы
- `id` - Уникальный идентификатор
- `name` - Название
- `population` - Население
- `geom` - Геометрия (Polygon, SRID 4326)
- `created_at` - Дата создания

**telegram_users** - Пользователи Telegram
- `id` - Уникальный идентификатор
- `telegram_id` - ID в Telegram
- `username` - Имя пользователя
- `chat_id` - ID чата
- `is_active` - Активен ли
- `created_at` - Дата регистрации

**user_subscriptions** - Подписки пользователей
- `id` - Уникальный идентификатор
- `user_id` - ID пользователя
- `district_id` - ID района
- `is_active` - Активна ли подписка
- `notification_time` - Время уведомлений

## 🤖 Telegram Bot

### Команды бота

- `/start` - Начать работу с ботом
- `/events` (или `/today`) - События сегодня в подписанных районах
- `/tomorrow` - События завтра
- `/week` - События на неделю
- `/districts` - Показать все районы
- `/subscribe <id>` - Подписаться на район
- `/unsubscribe <id>` - Отписаться от района
- `/myareas` - Мои подписки
- `/help` - Справка

### Возможности

- 📍 Подписка на районы города
- 🔔 Ежедневные уведомления о событиях (по умолчанию в 9:00)
- 🎭 Автоматический импорт событий с Яндекс.Афиши
- 📅 Проверка событий по запросу
- 💰 Отображение цен на события
- 🔗 Ссылки на источники событий

Подробная документация: [TELEGRAM_BOT_SETUP.md](TELEGRAM_BOT_SETUP.md)

## 🎭 Интеграция с Яндекс.Афишей

### Автоматический импорт

Система автоматически импортирует культурные события ежедневно в 2:00 (настраивается в `.env`).

### Настройка города

```env
YANDEX_AFISHA_CITY=voronezh  # moscow, spb, ekaterinburg, и т.д.
```

### Ручной импорт через API

```bash
curl -X POST "http://localhost:8000/api/events/import-afisha?city=voronezh&days_ahead=30&limit_per_category=50"
```

### Ручной импорт через Python

```bash
docker-compose exec backend python -c "
from app.scrapers.yandex_afisha import scrape_and_import_yandex_events
stats = scrape_and_import_yandex_events('voronezh')
print(f'Импортировано: {stats[\"imported\"]}, Дубликатов: {stats[\"duplicates\"]}')
"
```

### Импорт через веб-интерфейс

На главной странице нажмите кнопку **"📥 Импорт с Яндекс.Афиши"**

## 🌐 Использование веб-интерфейса

1. **Фильтр по типу** - Выберите тип события (концерты, театр, выставки и т.д.)
2. **Фильтр по дате** - Выберите период (сегодня, завтра, неделя, месяц)
3. **Фильтр по источнику** - Фильтруйте по источнику (Яндекс.Афиша, ручной ввод, Telegram)
4. **Слои карты** - Включайте/выключайте отображение событий и районов
5. **Информация о событии** - Кликните на маркер для просмотра деталей
6. **Импорт событий** - Используйте кнопку импорта для загрузки новых событий

## 🛠️ Стек технологий

### Backend
- **FastAPI** - Современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **GeoAlchemy2** - Расширение для геопространственных данных
- **Pydantic** - Валидация данных
- **python-telegram-bot** - Telegram Bot API
- **APScheduler** - Планировщик задач
- **Requests** - HTTP клиент для парсинга

### Frontend
- **Leaflet** - Библиотека для интерактивных карт
- **OpenStreetMap** - Картографические данные
- **Vanilla JavaScript** - Без фреймворков

### Database
- **PostgreSQL 15** - Реляционная БД
- **PostGIS 3.3** - Расширение для геопространственных данных

### Infrastructure
- **Docker** - Контейнеризация
- **Docker Compose** - Оркестрация контейнеров
- **Nginx** - Веб-сервер для frontend

## 📝 Примеры использования API

### Создание события

```bash
curl -X POST "http://localhost:8000/api/events/" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Концерт в филармонии",
    "event_type": "concert",
    "description": "Симфонический оркестр",
    "lat": 51.6606,
    "lon": 39.2003,
    "start_time": "2024-12-25T19:00:00",
    "end_time": "2024-12-25T21:00:00",
    "venue": "Воронежская филармония",
    "price": "от 500 ₽",
    "source": "manual"
  }'
```

### Получение событий сегодня

```bash
curl "http://localhost:8000/api/events/filter/today"
```

### Импорт с Яндекс.Афиши

```bash
curl -X POST "http://localhost:8000/api/events/import-afisha?city=voronezh&days_ahead=30"
```

## 🔄 Миграция с предыдущей версии

Если у вас была установлена предыдущая версия с объектами инфраструктуры:

```bash
# Применить миграцию (удаляет таблицу objects, добавляет новые поля в events)
docker-compose exec -T db psql -U geo_user -d city_geo_db < sql/migration_remove_objects.sql

# Загрузить новые тестовые данные
docker-compose exec -T db psql -U geo_user -d city_geo_db < sql/test_data.sql
```

Подробности в [REFACTORING_PROGRESS.md](REFACTORING_PROGRESS.md)

## 📚 Дополнительная документация

- [REFACTORING_PLAN.md](REFACTORING_PLAN.md) - План рефакторинга проекта
- [REFACTORING_PROGRESS.md](REFACTORING_PROGRESS.md) - Прогресс и детали изменений
- [TELEGRAM_BOT_SETUP.md](TELEGRAM_BOT_SETUP.md) - Настройка Telegram бота
- [QUICK_START.md](QUICK_START.md) - Быстрый старт для разработчиков

## 🐛 Устранение неполадок

### База данных не инициализируется

```bash
docker-compose down -v
docker-compose up -d
```

### Telegram бот не отвечает

1. Проверьте токен в `.env`
2. Проверьте логи: `docker-compose logs backend`
3. Убедитесь, что бот запущен: `docker-compose ps`

### События не импортируются

1. Проверьте настройки в `.env`
2. Проверьте логи: `docker-compose logs backend`
3. Попробуйте ручной импорт через API

## 📄 Лицензия

MIT License

## 👥 Авторы

- Разработка и рефакторинг: [Ваше имя]
- Оригинальная версия: [Burunduke](https://github.com/Burunduke)

## 🤝 Вклад в проект

Приветствуются pull requests! Для крупных изменений сначала откройте issue для обсуждения.